name: Materializar paquete Roma temporal

on:
  pull_request:
    branches:
      - main
    types: [opened, synchronize, reopened]
    paths:
      - scripts/fase_d/register_rome_pilot_master.py

permissions:
  contents: write

jobs:
  materializar:
    if: github.head_ref == 'agent/fase-d-registrar-paquete-roma-final'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/fase-d-registrar-paquete-roma-final
          token: ${{ secrets.GITHUB_TOKEN }}
      - name: Actualizar documento maestro
        run: python3 scripts/fase_d/register_rome_pilot_master.py
      - name: Confirmar registro único
        run: test "$(grep -c '67efcaa4e4cae2ec6f908f60a97850a1b7fd6ee223496fbc17438a87ea3a0550' __VIDA_INTERNACIONAL.md)" -eq 1
      - name: Retirar auxiliares y publicar rama final
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add __VIDA_INTERNACIONAL.md
          git rm scripts/fase_d/register_rome_pilot_master.py
          git rm .github/workflows/materializar-paquete-roma-temporal.yml
          git diff --cached --check
          git commit -m 'docs(fase-d): registrar paquete piloto validado de Roma'
          git push
