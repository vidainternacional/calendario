name: Validar importador transaccional de Jonás

on:
  pull_request:
    branches:
      - main
    paths:
      - "scripts/stepbible/generate_jonah_importer_draft.py"
      - "scripts/stepbible/test_jonah_importer_draft.py"
      - "scripts/stepbible/register_jonah_importer_master.py"
      - "supabase/migration-drafts/20260803060000_importador_payload_tahot_jonas.sql"
      - ".github/workflows/validate-jonah-importer-draft.yml"
      - "docs/FASE_D_IMPORTADOR_TAHOT_JONAS.md"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  validate:
    runs-on: ubuntu-24.04
    timeout-minutes: 40
    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: vida_jonah_importer_test
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U postgres -d vida_jonah_importer_test"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 10
    env:
      TEST_DATABASE_URL: postgresql://postgres:postgres@localhost:5432/vida_jonah_importer_test
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.head_ref || github.ref_name }}
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Instalar cliente PostgreSQL
        run: python -m pip install --disable-pip-version-check "psycopg[binary]==3.2.9"
      - name: Ejecutar auto-tests
        run: |
          python scripts/stepbible/tahot_schema.py --self-test
          python scripts/stepbible/extract_ot_book.py --self-test
          python scripts/stepbible/tahot_components.py
          python scripts/stepbible/build_tahot_import_payload.py --self-test
          python scripts/stepbible/generate_jonah_importer_draft.py --self-test
      - name: Generar paquete y payload canónico
        run: |
          python scripts/stepbible/extract_ot_book.py \
            --book Jon \
            --output artifacts/jonah-importer
          test "$(sha256sum artifacts/jonah-importer/jon.json.gz | cut -d' ' -f1)" = \
            "083b869fe7d10493deaeee392babd9811e9dffb91f0db816d2f21a22b2135915"
          python scripts/stepbible/build_tahot_import_payload.py \
            artifacts/jonah-importer/jon.json.gz \
            --policy scripts/stepbible/jonah_affix_lemma_policy.json \
            --output artifacts/jonah-importer/import-payload.json
          test "$(sha256sum artifacts/jonah-importer/import-payload.json | cut -d' ' -f1)" = \
            "e6bd082a446d29becbafb35a22b94ef9e260e447fe7fc7cea4361d98c5bb835b"
      - name: Verificar derivación exacta del borrador
        run: |
          test -f supabase/migration-drafts/20260803060000_importador_payload_tahot_jonas.sql
          python scripts/stepbible/generate_jonah_importer_draft.py \
            --active supabase/migrations/20260803043000_generalizar_importador_payload_tahot_nahum.sql \
            --output /tmp/importador_payload_tahot_jonas.sql
          cmp /tmp/importador_payload_tahot_jonas.sql \
            supabase/migration-drafts/20260803060000_importador_payload_tahot_jonas.sql
      - name: Validar rechazo, rollback e idempotencia
        run: |
          python scripts/stepbible/test_jonah_importer_draft.py \
            --dsn "$TEST_DATABASE_URL" \
            --fixture tests/sql/obadiah_textual_schema_fixture.sql \
            --base-migration supabase/migrations/20260802232000_importador_payload_tahot_obadias.sql \
            --ruth-migration supabase/migrations/20260803011000_generalizar_importador_payload_tahot_rut.sql \
            --haggai-migration supabase/migrations/20260803023000_generalizar_importador_payload_tahot_hageo.sql \
            --nahum-migration supabase/migrations/20260803043000_generalizar_importador_payload_tahot_nahum.sql \
            --draft supabase/migration-drafts/20260803060000_importador_payload_tahot_jonas.sql \
            --package artifacts/jonah-importer/jon.json.gz \
            --payload artifacts/jonah-importer/import-payload.json \
            --output artifacts/jonah-importer/importer-validation.json
          cat artifacts/jonah-importer/importer-validation.json
          git diff --check
      - uses: actions/upload-artifact@v4
        with:
          name: stepbible-jonah-importer-validation
          path: |
            supabase/migration-drafts/20260803060000_importador_payload_tahot_jonas.sql
            artifacts/jonah-importer/manifest.json
            artifacts/jonah-importer/importer-validation.json
          if-no-files-found: error
          retention-days: 14
      - name: Registrar validación en el maestro
        run: |
          if [ ! -f scripts/stepbible/register_jonah_importer_master.py ]; then
            echo "Auxiliar retirado; no hay actualización pendiente"
            exit 0
          fi
          python scripts/stepbible/register_jonah_importer_master.py
      - name: Versionar documento maestro
        run: |
          if git diff --quiet -- __VIDA_INTERNACIONAL.md; then
            echo "Sin cambios pendientes"
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add __VIDA_INTERNACIONAL.md
          git commit -m "docs(fase-d): registrar importador de Jonás"
          git push
