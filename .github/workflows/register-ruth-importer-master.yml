name: Registrar importador transaccional de Rut

on:
  pull_request:
    branches:
      - main
    paths:
      - "scripts/stepbible/register_ruth_importer_master.py"
      - ".github/workflows/register-ruth-importer-master.yml"

permissions:
  contents: write

jobs:
  update-master:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
        with:
          ref: fase-d/registrar-importador-rut
          fetch-depth: 0

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Actualizar documento maestro
        run: |
          python scripts/stepbible/register_ruth_importer_master.py
          git diff --check

      - name: Versionar actualización
        run: |
          if git diff --quiet; then exit 0; fi
          git config user.name "vida-internacional-bot"
          git config user.email "actions@users.noreply.github.com"
          git add __VIDA_INTERNACIONAL.md
          git commit -m "docs(fase-d): registrar importador transaccional de Rut"
          git push origin HEAD:fase-d/registrar-importador-rut
